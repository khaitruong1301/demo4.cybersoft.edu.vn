import tw, { styled } from 'twin.macro';

const Container = styled.header`
  ${tw`relative z-50 font-family[ Circular, -apple-system, BlinkMacSystemFont, Roboto, 'Helvetica Neue', sans-serif]`};
`;

export const HeaderCSS = {
  Container,
};
